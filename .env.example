# Environment variables for Docker Compose - Sniffer deployment
# Copy this file to .env and customize for your environment

# Grafana Credentials (CHANGE IN PRODUCTION!)
GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=your_secure_password_here

# Sniffer Configuration
RUST_LOG=info
SNIFFER_CONFIG=/app/config.toml

# ============================================
# STREAMING CONFIGURATION
# ============================================

# Streaming mode: "websocket" or "geyser"
STREAMING_MODE=websocket

# WebSocket URL (choose one based on your free tier provider)
# Native Solana (completely free, basic features)
SOLANA_WS_URL=wss://api.devnet.solana.com

# Helius Free Tier (100k requests/day, enhanced features)
# SOLANA_WS_URL=wss://devnet.helius-rpc.com/?api-key=YOUR_API_KEY

# QuickNode Free Tier (10M credits/month)
# SOLANA_WS_URL=wss://your-endpoint.solana-devnet.quiknode.pro/YOUR_TOKEN/

# Alchemy Free Tier (300M compute units/month)
# SOLANA_WS_URL=wss://solana-devnet.g.alchemy.com/v2/YOUR_API_KEY

# Geyser gRPC (premium, production use)
GRPC_ENDPOINT=https://your-geyser-endpoint:10000

# Optional: Database connection for persistence
# DATABASE_URL=postgresql://user:pass@host:5432/dbname

# Optional: Alert Manager endpoint
# ALERTMANAGER_URL=http://alertmanager:9093
